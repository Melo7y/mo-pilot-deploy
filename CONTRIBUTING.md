# 分支与发布约定

本约定适用于 `mo-pilot-frontend`、`mo-pilot-backend` 和 `mo-pilot-deploy`。三个仓库独立管理代码，统一发布产品版本；本文件由部署仓库维护。

## 日常开发

- `main` 是开发集成分支，接收已完成且验证通过的改动，可以包含尚未发布的功能。
- 从最新 `main` 创建短期分支：功能用 `feat/<名称>`，修复用 `fix/<名称>`，文档或维护用 `docs/<名称>`、`chore/<名称>`。
- 完成后通过 PR 合并到 `main`，再删除功能分支。提交前执行受影响项目要求的检查。
- 不常设 `dev`、`develop` 或迭代分支；任务文档记录目标版本。
- 跨仓库功能可以使用相同分支名，在 PR 中关联配套改动；分支不会跨仓库自动同步，发布前必须联合验证。

在需要修改的仓库中执行，先确认工作区干净：

```bash
git switch main
git pull --ff-only
git switch -c feat/export
```

生产环境只部署经过验证的明确版本镜像，不自动跟随 `main`。合并代码与部署上线是两个独立步骤。

## 产品发布

采用 `v0.1.0`、`v0.1.1`、`v0.2.0` 形式的版本标签。初期约定补丁版本用于问题修复，次版本用于下一批功能；涉及不兼容变化时必须在发布说明中明确说明。

1. 整理并提交本次发布的改动，确定前后端候选 commit，确保构建工作区干净。
2. 从候选 commit 构建镜像，执行项目检查和前后端联合冒烟测试，验证登录、核心业务流程和附件上传下载。
3. 在部署仓库新增 `releases/<版本>.yaml` 发布清单，记录前后端完整 commit SHA、镜像标签、目标平台、数据库迁移版本；镜像仓库交付记录 digest，离线交付记录镜像包 SHA256。发布清单必须记录实际验证结果，不能把未执行的检查标为通过。
4. 更新 `CHANGELOG.md`，记录功能变化、已知限制、升级步骤与数据库兼容性。将发布清单和部署配置提交后，在三个仓库已验证的提交上创建同名附注标签。未变化的仓库允许新标签指向原提交。
5. 推送对应提交和明确的标签，在部署仓库创建统一的 GitHub Release，附上部署包、镜像包及校验文件。

部署仓库自身的 commit 由产品标签确定，清单无需包含自身 commit SHA。发布清单和 CHANGELOG 在实际发布时创建，不用占位数据冒充已发布版本。

例如，在确认发布提交后，分别在各仓库执行（替换占位符）：

```bash
git tag -a v0.1.0 <已验证的commit> -m "MoPilot v0.1.0"
git push origin v0.1.0
```

对应镜像使用 `mopilot-api:v0.1.0`、`mopilot-web:v0.1.0`，部署环境设置 `MOPILOT_IMAGE_TAG=v0.1.0`。离线包使用 `mopilot-images-v0.1.0.tar.gz`、`mopilot-deploy-v0.1.0.tar.gz`。保留已验证的产物；已经发布的 Git 标签和同名镜像不得移动或覆盖，修复应使用新版本。

## 线上修复与并行验收

如果 `main` 已包含尚不能上线的新功能，从线上标签创建修复分支：

```bash
git switch -c hotfix/0.1.1 v0.1.0
```

在修复分支验证后发布 `v0.1.1`，并通过 PR 将修复同步回 `main` 及仍在维护的发布分支。其他未变化的仓库可以在旧发布提交上打 `v0.1.1` 标签，不能误用包含新功能的最新 `main`。

只有在一个版本验收期间还需要开发下个版本时，才从候选提交创建 `release/0.2`。该分支只接收本次发布修复，`main` 继续后续功能；修复必须同步回 `main`。验收通过后在发布分支打标签，不把包含后续功能的 `main` 合入发布分支。发布结束且修复全部同步后，无持续维护需求即可删除该分支。

升级前备份数据库与附件。只有数据库兼容旧应用时才能直接回滚镜像；不兼容迁移按部署 README 的恢复流程处理。
